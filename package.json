{
  "name": "core",
  "private": false,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "build": "lerna run build",
    "test": "lerna run test",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "@types/node": "^20.13.0",
    "lerna": "^8.1.3",
    "typescript": "^5.4.5"
  }
}
